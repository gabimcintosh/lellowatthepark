{
  "name": "lellowatthepark",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "yarn encode && vite",
    "build": "yarn encode && tsc && vite build",
    "preview": "vite preview",
    "encode": "node scripts/encode.cjs"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.3.1"
  },
  "packageManager": "yarn@4.9.1",
  "dependencies": {
    "@msgpack/msgpack": "^3.1.1"
  }
}
